version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: arbitraje_app
    working_dir: /var/www
    volumes:
      - ./arbitraje:/var/www
    networks:
      - arbitraje_network
      - red_general

  webserver:
    image: nginx:alpine
    container_name: arbitraje_server
    ports:
      - "8088:80"
    volumes:
      - ./arbitraje:/var/www
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
    networks:
      - arbitraje_network
      - red_general

networks:
  arbitraje_network:
    driver: bridge
  red_general:
    external: true
